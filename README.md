# Road-PyBuilder
PyBuilder — a lightweight Tkinter GUI that turns Python scripts into Windows .exe files using PyInstaller (one‑file, no console, icon support). PyBuilder — лёгкое GUI-приложение на Tkinter для сборки Python-скриптов в Windows .exe через PyInstaller (onefile, без консоли, поддержка иконки).
